# 릴리스 전용 version bump 정책

## 목표

일반 문서·directive·작업 기록 변경이 `main`의 package version을 올리지 않도록 규칙을 정정하고, 불필요하게 추가된 `0.12.180` 표기를 제거한다.

## 수행 결과

- [x] version과 commit message suffix 규칙을 릴리스 전용으로 변경했다.
- [x] `0.12.180` package 및 changelog 표기를 제거했다.
- [x] 변경을 검증하고 `origin/main`에 push했다.
