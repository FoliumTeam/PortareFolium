# Changelog: 2026-08-12

## Web Resume 소개와 직무별 공개 반영

- Web Resume 소개를 교차로 edge computer 차량 데이터 기반 통행배정 API 서버의 재설계·준공, 대규모 처리 안정화, 미완성 외주 codebase 인수 경험 중심으로 개편
- Portfolio·Blog·Resume·About Me 저장 시 `/web`·`/game` 공개 경로까지 cache revalidation하도록 보강
- Published Portfolio 편집본은 Draft로 저장되고 이전 공개본이 유지된다는 상태를 관리자 editor와 저장 결과에 명시
- 인증 Chromium에서 Portfolio editor의 공개 반영 안내와 browser runtime 오류 부재를 검증
- Portfolio는 저장 직후 즉시 Published하며 Draft·검토·승인·발행 workflow와 공개 snapshot 차단을 제거

## Resume 대표 프로젝트 편집성과 제목 규칙

- Web Resume의 대표 프로젝트 제목을 공통 label helper로 통합해 emoji가 항상 `🚀 대표 프로젝트`처럼 맨 앞에 표시되도록 수정
- 관리자 Resume 상단에 대표 프로젝트 편집 바로가기를 추가하고 공개 layout에서 disabled인 section도 관리자에서는 계속 편집할 수 있도록 분리
- 대표 프로젝트 선정·순서와 이력서 설명의 관리 위치를 editor 안에 안내하고 인증 E2E로 편집 구역 이동과 browser 오류 부재를 검증
- Resume·Portfolio·About Me·Blog 및 원칙상 Landing page까지 공개 콘텐츠를 Admin Dashboard에서 편집해야 한다는 architecture 계약 추가

## Resume 기술 브랜드 표시

- Resume 기술 keyword에 DB 기반 `iconUrl`을 추가하고 관리자 편집·미리보기·목록에서 같은 logo source를 사용하도록 연결
- Java는 OpenJDK, Cloudflare R2·SQLite3·Synology NAS API·MCP는 Simple Icons의 검증된 slug와 brand color로 지정
- Amazon S3·Zustand·NextAuth는 공식 자산을 Cloudflare R2에 저장하고 Next.js image optimization을 거쳐 화면과 PDF preview에서 같은 logo를 표시
- DB 전체 snapshot 후 보라색 fallback 8개를 갱신하고 Web·Game Resume 분리, logo 로딩, PDF 생성·다운로드를 자동화 검사로 확인

## Portfolio article media와 목차 경계

- Portfolio 대표 image gallery의 제목을 `대표 이미지`로 통일하고 손상된 Portare Folium 화면을 새 16:9 capture와 lightbox thumbnail로 교체
- 최적화 thumbnail이 없을 때 원본 image로 fallback해 filmstrip 탐색이 깨지지 않도록 보강
- 우측 중앙 고정 목차가 article body 안에서 시작하도록 sticky 범위를 분리해 대표 이미지 영역과의 충돌 제거
- Portare Folium article에 문맥별 image, blockquote, table, code tree, list를 배치하고 작성 guide·관리자 prompt·design directive에 같은 원칙 반영
- 로컬 운영용 `docs/private` script를 product TypeScript build 범위에서 제외

## Carevia JD 근거 보강

- 외주사가 10개월간 진행한 미완성 통행배정 codebase를 마감 2개월 전에 인수해 핵심 로직을 추출하고 FastAPI 서버로 전면 재설계·준공한 경력 사실을 이력서와 Portfolio에 반영
- 기존 Spring Boot 연계 API 서버의 동작·통신 규격을 확인하며 배포한 경험을 `기초` 수준으로 한정하고 Spring Security·JPA·QueryDSL·MySQL 경험은 추가하지 않음
- AI 팀의 실제 파일 업무 순서를 업로드·상태 확인·자동 분류·탐색 흐름으로 설계한 과정을 AI 파일 관리 Portfolio에 보강
- Portare Folium의 공식 WCAG 검수 여부와 읽기 피로·클릭 수를 줄인 UI 판단을 구분해 기술
- WebView·hybrid app·native push·offline sync·WebSocket 구현 여부를 source와 dependencies에서 점검하고 경험 근거가 없음을 확인

## 웹 지원 자료 채용담당자 관점 개선

- 웹 이력서의 프로젝트 8개 card를 Featured 대표 3건의 direct link와 전체 Portfolio 진입으로 압축
- 기술 view selector와 동일 높이 card를 제거하고 category별 가로 행, inline 숙련도, 채워진 fallback badge로 재설계
- Portfolio 이전 기록의 펼치기 toggle을 제거하고 tablet 이상 우측 중앙 고정 목차와 한국어 안내 문구 적용
- Portare Folium에 App Router 구조, 관리자 운영 기능, 실제 editor 화면을 추가하고 주요 실무 Portfolio의 역할·문제 해결·결과 서술 보강
- `Nexon Open API`를 기술 목록에서 제거하고 경력·핵심 역량·게임 profile 데이터는 기존 사실 그대로 보존

## 분야별 공개 경로 E2E 정합성

- Resume, Portfolio, Blog E2E가 분야 선택 화면이 아닌 `/web` 공개 콘텐츠 경로를 검증하도록 갱신
- 인증 setup과 PDF export 검증의 사전 로드 경로를 web profile 계약에 맞게 정리
- navigation E2E의 클릭과 URL 전환 대기를 결합해 hydration 시점의 간헐 실패 방지

## 웹 이력서·포트폴리오 사실 정합성

- 웹 소개와 핵심 역량을 게임 프로필과 독립된 직무 범위로 갱신하고 기존 게임 소개·핵심 역량은 보존
- 웹 이력서 프로젝트를 최신 웹 포트폴리오 7건과 ROI 도구까지 8건으로 정리하고 상세 페이지 연결·역할·결과 보강
- Portare Folium의 Supabase·Cloudflare R2·NextAuth·MCP 등 최신 웹 기술을 기술 섹션에 반영
- 이력서 경력 섹션을 사실 정본으로 유지하고 ITS 처리량, AI 파일 관리 효율, 클라이언트 사이트 성과를 관련 포트폴리오에 반영

## v0.12.189

- Portfolio와 Resume 프로젝트 카드의 전체 클릭 영역을 프로젝트 기록으로 연결하고 외부 source 행동은 분리
- Portfolio 행동 버튼을 한국어로 통일하고 `Case Study`를 프로젝트 기록으로 교체
- Portare Folium 사례에 직무별 공개 화면 캡처, 관리자 중심 운영 기능, 공개 안정성 문제 해결 과정을 보강

## v0.12.188

- game과 web의 사례 연구 heading 계약과 작성 template을 분리하고 game 사례의 Unreal Portfolio 형식을 적용
- game·web Featured 상태와 순서를 `data`의 직무 분야별 map으로 분리해 복수 직무 프로젝트를 독립 관리
- 관리자 Portfolio 화면에 Featured scope 선택과 분야별 순서 목록을 추가

## v0.12.187

- Portfolio 사례 연구를 배경·내 역할·만든 과정·결과·회고 중심의 서사형 구조로 전환하고 기존 공개 사례 heading을 호환 표시
- 관리자 Ready for Review 화면에 공통 content width와 반응형 여백을 적용
- Portfolio Featured 최대 5개와 순서 조정을 직무 분야별로 분리
