# Changelog: 2026-05-03

## v0.12.128

### chore: SQL 기반 migration catalog 생성 추가

- `supabase/migrations/`: 기존 migration catalog SQL을 `001_`부터 순번이 있는 개별 SQL 파일로 분리.
- `scripts/generate-migrations.ts`: SQL 파일 header와 본문을 검증해 `src/lib/migrations.ts`를 생성하는 build-time generator 추가.
- `package.json`, `src/lib/migrations.ts`: `dev`/`build` 전에 migration catalog를 재생성하고 runtime은 생성된 TS catalog를 사용하도록 전환.
